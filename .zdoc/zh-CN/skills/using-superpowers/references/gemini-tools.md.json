{
  "sourceFile": "skills/using-superpowers/references/gemini-tools.md",
  "sourceLanguage": "en-US",
  "entries": [
    {
      "en-US": "# Gemini CLI Tool Mapping",
      "zh-Hans": "# Gemini CLI 工具映射"
    },
    {
      "en-US": "Skills use Claude Code tool names. When you encounter these in a skill, use your platform equivalent:",
      "zh-Hans": "技能使用 Claude Code 工具名称。当您在技能中遇到这些名称时，请使用您平台上的等效工具："
    },
    {
      "en-US": "| Skill references | Gemini CLI equivalent |\n|-----------------|----------------------|\n| `Read` (file reading) | `read_file` |\n| `Write` (file creation) | `write_file` |\n| `Edit` (file editing) | `replace` |\n| `Bash` (run commands) | `run_shell_command` |\n| `Grep` (search file content) | `grep_search` |\n| `Glob` (search files by name) | `glob` |\n| `TodoWrite` (task tracking) | `write_todos` |\n| `Skill` tool (invoke a skill) | `activate_skill` |\n| `WebSearch` | `google_web_search` |\n| `WebFetch` | `web_fetch` |\n| `Task` tool (dispatch subagent) | `@agent-name` (see [Subagent support](#subagent-support)) |",
      "zh-Hans": "| 技能参考 | Gemini CLI 等效项 |\n|-----------------|----------------------|\n| `Read` (读取文件) | `read_file` |\n| `Write` (创建文件) | `write_file` |\n| `Edit` (编辑文件) | `replace` |\n| `Bash` (运行命令) | `run_shell_command` |\n| `Grep` (搜索文件内容) | `grep_search` |\n| `Glob` (按名称搜索文件) | `glob` |\n| `TodoWrite` (任务追踪) | `write_todos` |\n| `Skill` 工具 (调用技能) | `activate_skill` |\n| `WebSearch` | `google_web_search` |\n| `WebFetch` | `web_fetch` |\n| `Task` 工具 (调度子代理) | `@agent-name` (参见 [子代理支持](#子代理支持)) |"
    },
    {
      "en-US": "## Subagent support",
      "zh-Hans": "## 子代理支持"
    },
    {
      "en-US": "Gemini CLI supports subagents natively via the `@` syntax. Use the built-in `@generalist` agent to dispatch any task — it has access to all tools and follows the prompt you provide.",
      "zh-Hans": "Gemini CLI 通过 `@` 语法原生支持子代理。使用内置的 `@generalist` 代理来分派任何任务——它可以访问所有工具，并且会遵循您提供的提示。"
    },
    {
      "en-US": "When a skill says to dispatch a named agent type, use `@generalist` with the full prompt from the skill's prompt template:",
      "zh-Hans": "当技能指示分派一个命名代理类型时，使用 `@generalist` 并附带来自该技能提示模板的完整提示："
    },
    {
      "en-US": "| Skill instruction | Gemini CLI equivalent |\n|-------------------|----------------------|\n| `Task tool (superpowers:implementer)` | `@generalist` with the filled `implementer-prompt.md` template |\n| `Task tool (superpowers:spec-reviewer)` | `@generalist` with the filled `spec-reviewer-prompt.md` template |\n| `Task tool (superpowers:code-reviewer)` | `@code-reviewer` (bundled agent) or `@generalist` with the filled review prompt |\n| `Task tool (superpowers:code-quality-reviewer)` | `@generalist` with the filled `code-quality-reviewer-prompt.md` template |\n| `Task tool (general-purpose)` with inline prompt | `@generalist` with your inline prompt |",
      "zh-Hans": "| 技能指令 | Gemini CLI 等效项 |\n|-------------------|----------------------|\n| `Task tool (superpowers:implementer)` | `@generalist` 并使用已填充的 `implementer-prompt.md` 模板 |\n| `Task tool (superpowers:spec-reviewer)` | `@generalist` 并使用已填充的 `spec-reviewer-prompt.md` 模板 |\n| `Task tool (superpowers:code-reviewer)` | `@code-reviewer` (捆绑代理) 或 `@generalist` 并使用已填充的审查提示 |\n| `Task tool (superpowers:code-quality-reviewer)` | `@generalist` 并使用已填充的 `code-quality-reviewer-prompt.md` 模板 |\n| `Task tool (general-purpose)` 内联提示 | `@generalist` 并使用您的内联提示 |"
    },
    {
      "en-US": "### Prompt filling",
      "zh-Hans": "### 提示填充"
    },
    {
      "en-US": "Skills provide prompt templates with placeholders like `{WHAT_WAS_IMPLEMENTED}` or `[FULL TEXT of task]`. Fill all placeholders and pass the complete prompt as the message to `@generalist`. The prompt template itself contains the agent's role, review criteria, and expected output format — `@generalist` will follow it.",
      "zh-Hans": "技能提供的提示模板包含诸如 `{WHAT_WAS_IMPLEMENTED}` 或 `[FULL TEXT of task]` 之类的占位符。填充所有占位符，并将完整的提示作为消息传递给 `@generalist`。提示模板本身包含了代理的角色、审查标准和预期的输出格式——`@generalist` 将遵循它。"
    },
    {
      "en-US": "### Parallel dispatch",
      "zh-Hans": "### 并行分派"
    },
    {
      "en-US": "Gemini CLI supports parallel subagent dispatch. When a skill asks you to dispatch multiple independent subagent tasks in parallel, request all of those `@generalist` or named subagent tasks together in the same prompt. Keep dependent tasks sequential, but do not serialize independent subagent tasks just to preserve a simpler history.",
      "zh-Hans": "Gemini CLI 支持并行子代理分派。当技能要求您并行分派多个独立的子代理任务时，在同一个提示中一起请求所有的 `@generalist` 或命名的子代理任务。保持存在依赖关系的任务顺序执行，但不要仅仅为了保持历史记录简洁而对独立的子代理任务进行串行化。"
    },
    {
      "en-US": "## Additional Gemini CLI tools",
      "zh-Hans": "## 额外的 Gemini CLI 工具"
    },
    {
      "en-US": "These tools are available in Gemini CLI but have no Claude Code equivalent:",
      "zh-Hans": "这些工具在 Gemini CLI 中可用，但没有 Claude Code 的等效工具："
    },
    {
      "en-US": "| Tool | Purpose |\n|------|---------|\n| `list_directory` | List files and subdirectories |\n| `save_memory` | Persist facts to GEMINI.md across sessions |\n| `ask_user` | Request structured input from the user |\n| `tracker_create_task` | Rich task management (create, update, list, visualize) |\n| `enter_plan_mode` / `exit_plan_mode` | Switch to read-only research mode before making changes |",
      "zh-Hans": "| 工具 | 用途 |\n|------|---------|\n| `list_directory` | 列出文件和子目录 |\n| `save_memory` | 跨会话将事实持久化到 GEMINI.md |\n| `ask_user` | 向用户请求结构化输入 |\n| `tracker_create_task` | 丰富的任务管理（创建、更新、列出、可视化） |\n| `enter_plan_mode` / `exit_plan_mode` | 在进行更改之前切换到只读研究模式 |"
    }
  ]
}