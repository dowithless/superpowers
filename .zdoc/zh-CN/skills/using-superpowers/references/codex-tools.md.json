{
  "sourceFile": "skills/using-superpowers/references/codex-tools.md",
  "sourceLanguage": "en-US",
  "entries": [
    {
      "en-US": "# Codex Tool Mapping",
      "zh-Hans": "# Codex 工具映射"
    },
    {
      "en-US": "Skills use Claude Code tool names. When you encounter these in a skill, use your platform equivalent:",
      "zh-Hans": "技能使用 Claude Code 工具名称。当您在技能中遇到这些时，请使用您平台的等效工具："
    },
    {
      "en-US": "| Skill references | Codex equivalent |\n|-----------------|------------------|\n| `Task` tool (dispatch subagent) | `spawn_agent` (see [Subagent dispatch requires multi-agent support](#subagent-dispatch-requires-multi-agent-support)) |\n| Multiple `Task` calls (parallel) | Multiple `spawn_agent` calls |\n| Task returns result | `wait_agent` |\n| Task completes automatically | `close_agent` to free slot |\n| `TodoWrite` (task tracking) | `update_plan` |\n| `Skill` tool (invoke a skill) | Skills load natively — just follow the instructions |\n| `Read`, `Write`, `Edit` (files) | Use your native file tools |\n| `Bash` (run commands) | Use your native shell tools |",
      "zh-Hans": "| 技能参考 | Codex 等价项 |\n|-----------------|------------------|\n| `Task` 工具（分发子代理） | `spawn_agent`（参见[子代理分发需要多代理支持](#子代理分发需要多代理支持)） |\n| 多次 `Task` 调用（并行） | 多次 `spawn_agent` 调用 |\n| 任务返回结果 | `wait_agent` |\n| 任务自动完成 | `close_agent` 释放插槽 |\n| `TodoWrite`（任务跟踪） | `update_plan` |\n| `Skill` 工具（调用技能） | 技能原生加载——只需遵循指示 |\n| `Read`、`Write`、`Edit`（文件） | 使用原生文件工具 |\n| `Bash`（运行命令） | 使用原生 shell 工具 |"
    },
    {
      "en-US": "## Subagent dispatch requires multi-agent support",
      "zh-Hans": "## 子代理分发需要多代理支持"
    },
    {
      "en-US": "Add to your Codex config (`~/.codex/config.toml`):",
      "zh-Hans": "添加到您的 Codex 配置（`~/.codex/config.toml`）："
    },
    {
      "en-US": "```toml\n[features]\nmulti_agent = true\n```",
      "zh-Hans": "```toml\n[features]\nmulti_agent = true\n```"
    },
    {
      "en-US": "This enables `spawn_agent`, `wait_agent`, and `close_agent` for skills like `dispatching-parallel-agents` and `subagent-driven-development`.",
      "zh-Hans": "这为诸如 `dispatching-parallel-agents` 和 `subagent-driven-development` 之类的技能启用了 `spawn_agent`、`wait_agent` 和 `close_agent`。"
    },
    {
      "en-US": "Legacy note: Codex builds before `rust-v0.115.0` exposed spawned-agent\nwaiting as `wait`. Current Codex uses `wait_agent` for spawned agents. The\n`wait` name now belongs to code-mode `exec/wait`, which resumes a yielded exec\ncell by `cell_id`; it is not the spawned-agent result tool.",
      "zh-Hans": "遗留说明：`rust-v0.115.0` 之前的 Codex 版本将生成的代理等待暴露为 `wait`。当前 Codex 对生成的代理使用 `wait_agent`。`wait` 这个名称现在属于代码模式下的 `exec/wait`，通过 `cell_id` 恢复已挂起的执行单元格；它不是生成的代理结果工具。"
    },
    {
      "en-US": "## Environment Detection",
      "zh-Hans": "## 环境检测"
    },
    {
      "en-US": "Skills that create worktrees or finish branches should detect their\nenvironment with read-only git commands before proceeding:",
      "zh-Hans": "创建 worktree 或完成分支的技能应在继续之前，使用只读 git 命令检测其环境："
    },
    {
      "en-US": "```bash\nGIT_DIR=$(cd \"$(git rev-parse --git-dir)\" 2>/dev/null && pwd -P)\nGIT_COMMON=$(cd \"$(git rev-parse --git-common-dir)\" 2>/dev/null && pwd -P)\nBRANCH=$(git branch --show-current)\n```",
      "zh-Hans": "```bash\nGIT_DIR=$(cd \"$(git rev-parse --git-dir)\" 2>/dev/null && pwd -P)\nGIT_COMMON=$(cd \"$(git rev-parse --git-common-dir)\" 2>/dev/null && pwd -P)\nBRANCH=$(git branch --show-current)\n```"
    },
    {
      "en-US": "- `GIT_DIR != GIT_COMMON` → already in a linked worktree (skip creation)\n- `BRANCH` empty → detached HEAD (cannot branch/push/PR from sandbox)",
      "zh-Hans": "* `GIT_DIR != GIT_COMMON` → 已在链接的 worktree 中（跳过创建）\n* `BRANCH` 为空 → 分离的 HEAD（无法从沙盒中分支/推送/PR）"
    },
    {
      "en-US": "See `using-git-worktrees` Step 0 and `finishing-a-development-branch`\nStep 1 for how each skill uses these signals.",
      "zh-Hans": "有关每个技能如何使用这些信号的示例，请参见 `using-git-worktrees` 第 0 步和 `finishing-a-development-branch` 第 1 步。"
    },
    {
      "en-US": "## Codex App Finishing",
      "zh-Hans": "## Codex App 完成"
    },
    {
      "en-US": "When the sandbox blocks branch/push operations (detached HEAD in an\nexternally managed worktree), the agent commits all work and informs\nthe user to use the App's native controls:",
      "zh-Hans": "当沙盒阻止分支/推送操作时（在外部管理的 worktree 中处于分离的 HEAD 状态），代理将提交所有工作并通知用户使用 App 的原生控件："
    },
    {
      "en-US": "- **\"Create branch\"** — names the branch, then commit/push/PR via App UI\n- **\"Hand off to local\"** — transfers work to the user's local checkout",
      "zh-Hans": "* **\"创建分支\"** —— 命名分支，然后通过 App UI 提交/推送/PR\n* **\"移交到本地\"** —— 将工作转移到用户的本地检出"
    },
    {
      "en-US": "The agent can still run tests, stage files, and output suggested branch\nnames, commit messages, and PR descriptions for the user to copy.",
      "zh-Hans": "代理仍然可以运行测试、暂存文件，并输出建议的分支名称、提交消息和 PR 描述供用户复制。"
    }
  ]
}