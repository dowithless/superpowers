{
  "sourceFile": "docs/superpowers/specs/2026-03-11-zero-dep-brainstorm-server-design.md",
  "sourceLanguage": "en-US",
  "entries": [
    {
      "en-US": "# Zero-Dependency Brainstorm Server",
      "zh-Hans": "# 零依赖头脑风暴服务器"
    },
    {
      "en-US": "Replace the brainstorm companion server's vendored node_modules (express, ws, chokidar — 714 tracked files) with a single zero-dependency `server.js` using only Node.js built-ins.",
      "zh-Hans": "将头脑风暴伴侣服务器中打包的 node\\_modules（express、ws、chokidar —— 714 个跟踪文件）替换为仅使用 Node.js 内置模块的单个零依赖 `server.js`。"
    },
    {
      "en-US": "## Motivation",
      "zh-Hans": "## 动机"
    },
    {
      "en-US": "Vendoring node_modules into the git repo creates a supply chain risk: frozen dependencies don't get security patches, 714 files of third-party code are committed without audit, and modifications to vendored code look like normal commits. While the actual risk is low (localhost-only dev server), eliminating it is straightforward.",
      "zh-Hans": "将 node\\_modules 打包到 git 仓库中会带来供应链风险：冻结的依赖项无法获得安全补丁，714 个未经审计的第三方代码文件被提交，且对打包代码的修改看起来像普通提交。虽然实际风险较低（仅限本地开发服务器），但消除它非常简单。"
    },
    {
      "en-US": "## Architecture",
      "zh-Hans": "## 架构"
    },
    {
      "en-US": "A single `server.js` file (~250-300 lines) using `http`, `crypto`, `fs`, and `path`. The file serves two roles:",
      "zh-Hans": "使用 `http`、`crypto`、`fs` 和 `path` 的单个 `server.js` 文件（约 250-300 行）。该文件扮演两个角色："
    },
    {
      "en-US": "- **When run directly** (`node server.js`): starts the HTTP/WebSocket server\n- **When required** (`require('./server.js')`): exports WebSocket protocol functions for unit testing",
      "zh-Hans": "* **直接运行时**（`node server.js`）：启动 HTTP/WebSocket 服务器\n* **被引用时**（`require('./server.js')`）：导出用于单元测试的 WebSocket 协议函数"
    },
    {
      "en-US": "### WebSocket Protocol",
      "zh-Hans": "### WebSocket 协议"
    },
    {
      "en-US": "Implements RFC 6455 for text frames only:",
      "zh-Hans": "仅实现文本帧的 RFC 6455："
    },
    {
      "en-US": "**Handshake:** Compute `Sec-WebSocket-Accept` from client's `Sec-WebSocket-Key` using SHA-1 + the RFC 6455 magic GUID. Return 101 Switching Protocols.",
      "zh-Hans": "**握手：** 使用 SHA-1 和 RFC 6455 魔法 GUID 从客户端的 `Sec-WebSocket-Key` 计算 `Sec-WebSocket-Accept`。返回 101 切换协议。"
    },
    {
      "en-US": "**Frame decoding (client to server):** Handle three masked length encodings:",
      "zh-Hans": "**帧解码（客户端到服务器）：** 处理三种带掩码的长度编码："
    },
    {
      "en-US": "- Small: payload < 126 bytes\n- Medium: 126-65535 bytes (16-bit extended)\n- Large: > 65535 bytes (64-bit extended)",
      "zh-Hans": "* 小型：有效负载 < 126 字节\n* 中型：126-65535 字节（16 位扩展）\n* 大型：> 65535 字节（64 位扩展）"
    },
    {
      "en-US": "XOR-unmask payload using 4-byte mask key. Return `{ opcode, payload, bytesConsumed }` or `null` for incomplete buffers. Reject unmasked frames.",
      "zh-Hans": "使用 4 字节掩码密钥对有效负载进行 XOR 解掩码。对于不完整的缓冲区，返回 `{ opcode, payload, bytesConsumed }` 或 `null`。拒绝未掩码的帧。"
    },
    {
      "en-US": "**Frame encoding (server to client):** Unmasked frames with the same three length encodings.",
      "zh-Hans": "**帧编码（服务器到客户端）：** 使用相同三种长度编码的未掩码帧。"
    },
    {
      "en-US": "**Opcodes handled:** TEXT (0x01), CLOSE (0x08), PING (0x09), PONG (0x0A). Unrecognized opcodes get a close frame with status 1003 (Unsupported Data).",
      "zh-Hans": "**处理的操作码：** TEXT (0x01)、CLOSE (0x08)、PING (0x09)、PONG (0x0A)。无法识别的操作码将收到状态为 1003（不支持的数据）的关闭帧。"
    },
    {
      "en-US": "**Deliberately skipped:** Binary frames, fragmented messages, extensions (permessage-deflate), subprotocols. These are unnecessary for small JSON text messages between localhost clients. Extensions and subprotocols are negotiated in the handshake — by not advertising them, they are never active.",
      "zh-Hans": "**有意跳过的功能：** 二进制帧、分片消息、扩展（permessage-deflate）、子协议。这些对于 localhost 客户端之间的小型 JSON 文本消息是不必要的。扩展和子协议在握手时协商 —— 通过不宣告它们，它们永远不会被激活。"
    },
    {
      "en-US": "**Buffer accumulation:** Each connection maintains a buffer. On `data`, append and loop `decodeFrame` until it returns null or buffer is empty.",
      "zh-Hans": "**缓冲区累积：** 每个连接维护一个缓冲区。在 `data` 时，追加并循环 `decodeFrame`，直到它返回 null 或缓冲区为空。"
    },
    {
      "en-US": "### HTTP Server",
      "zh-Hans": "### HTTP 服务器"
    },
    {
      "en-US": "Three routes:",
      "zh-Hans": "三个路由："
    },
    {
      "en-US": "1. **`GET /`** — Serve newest `.html` from screen directory by mtime. Detect full documents vs fragments, wrap fragments in frame template, inject helper.js. Return `text/html`. When no `.html` files exist, serve a hardcoded waiting page (\"Waiting for Claude to push a screen...\") with helper.js injected.\n2. **`GET /files/*`** — Serve static files from screen directory with MIME type lookup from a hardcoded extension map (html, css, js, png, jpg, gif, svg, json). Return 404 if not found.\n3. **Everything else** — 404.",
      "zh-Hans": "1. **`GET /`** —— 按修改时间从屏幕目录提供最新的 `.html`。检测完整文档与片段，将片段包装在框架模板中，注入 helper.js。返回 `text/html`。当不存在 `.html` 文件时，提供一个硬编码的等待页面（\"等待 Claude 推送屏幕...\"）并注入 helper.js。\n2. **`GET /files/*`** —— 从屏幕目录提供静态文件，使用硬编码的扩展名映射（html、css、js、png、jpg、gif、svg、json）查找 MIME 类型。如果未找到则返回 404。\n3. **其他所有请求** —— 404。"
    },
    {
      "en-US": "WebSocket upgrade handled via the `'upgrade'` event on the HTTP server, separate from the request handler.",
      "zh-Hans": "WebSocket 升级通过 HTTP 服务器上的 `'upgrade'` 事件处理，与请求处理器分开。"
    },
    {
      "en-US": "### Configuration",
      "zh-Hans": "### 配置"
    },
    {
      "en-US": "Environment variables (all optional):",
      "zh-Hans": "环境变量（全部可选）："
    },
    {
      "en-US": "- `BRAINSTORM_PORT` — port to bind (default: random high port 49152-65535)\n- `BRAINSTORM_HOST` — interface to bind (default: `127.0.0.1`)\n- `BRAINSTORM_URL_HOST` — hostname for the URL in startup JSON (default: `localhost` when host is `127.0.0.1`, otherwise same as host)\n- `BRAINSTORM_DIR` — screen directory path (default: `/tmp/brainstorm`)",
      "zh-Hans": "* `BRAINSTORM_PORT` —— 绑定的端口（默认：随机高端口 49152-65535）\n* `BRAINSTORM_HOST` —— 绑定的接口（默认：`127.0.0.1`）\n* `BRAINSTORM_URL_HOST` —— 启动 JSON 中 URL 的主机名（默认：当主机为 `127.0.0.1` 时为 `localhost`，否则与主机相同）\n* `BRAINSTORM_DIR` —— 屏幕目录路径（默认：`/tmp/brainstorm`）"
    },
    {
      "en-US": "### Startup Sequence",
      "zh-Hans": "### 启动顺序"
    },
    {
      "en-US": "1. Create `SCREEN_DIR` if it doesn't exist (`mkdirSync` recursive)\n2. Load frame template and helper.js from `__dirname`\n3. Start HTTP server on configured host/port\n4. Start `fs.watch` on `SCREEN_DIR`\n5. On successful listen, log `server-started` JSON to stdout: `{ type, port, host, url_host, url, screen_dir }`\n6. Write the same JSON to `SCREEN_DIR/.server-info` so agents can find connection details when stdout is hidden (background execution)",
      "zh-Hans": "1. 如果不存在则创建 `SCREEN_DIR`（`mkdirSync` 递归）\n2. 从 `__dirname` 加载框架模板和 helper.js\n3. 在配置的主机/端口上启动 HTTP 服务器\n4. 在 `SCREEN_DIR` 上启动 `fs.watch`\n5. 成功监听后，将 `server-started` JSON 记录到 stdout：`{ type, port, host, url_host, url, screen_dir }`\n6. 将相同的 JSON 写入 `SCREEN_DIR/.server-info`，以便在 stdout 被隐藏时（后台执行）代理可以找到连接详细信息"
    },
    {
      "en-US": "### Application-Level WebSocket Messages",
      "zh-Hans": "### 应用层 WebSocket 消息"
    },
    {
      "en-US": "When a TEXT frame arrives from a client:",
      "zh-Hans": "当从客户端收到 TEXT 帧时："
    },
    {
      "en-US": "1. Parse as JSON. If parsing fails, log to stderr and continue.\n2. Log to stdout as `{ source: 'user-event', ...event }`.\n3. If the event contains a `choice` property, append the JSON to `SCREEN_DIR/.events` (one line per event).",
      "zh-Hans": "1. 解析为 JSON。如果解析失败，记录到 stderr 并继续。\n2. 记录到 stdout 为 `{ source: 'user-event', ...event }`。\n3. 如果事件包含 `choice` 属性，则将 JSON 追加到 `SCREEN_DIR/.events`（每个事件一行）。"
    },
    {
      "en-US": "### File Watching",
      "zh-Hans": "### 文件监视"
    },
    {
      "en-US": "`fs.watch(SCREEN_DIR)` replaces chokidar. On HTML file events:",
      "zh-Hans": "`fs.watch(SCREEN_DIR)` 替换 chokidar。在 HTML 文件事件上："
    },
    {
      "en-US": "- On new file (`rename` event for a file that exists): delete `.events` file if present (`unlinkSync`), log `screen-added` to stdout as JSON\n- On file change (`change` event): log `screen-updated` to stdout as JSON (do NOT clear `.events`)\n- Both events: send `{ type: 'reload' }` to all connected WebSocket clients",
      "zh-Hans": "* 新文件时（存在文件的 `rename` 事件）：如果存在则删除 `.events` 文件（`unlinkSync`），将 `screen-added` 记录到 stdout 为 JSON\n* 文件更改时（`change` 事件）：将 `screen-updated` 记录到 stdout 为 JSON（不 清除 `.events`）\n* 两个事件：将 `{ type: 'reload' }` 发送给所有连接的 WebSocket 客户端"
    },
    {
      "en-US": "Debounce per-filename with ~100ms timeout to prevent duplicate events (common on macOS and Linux).",
      "zh-Hans": "使用约 100 毫秒的超时按文件名进行防抖，以防止重复事件（在 macOS 和 Linux 上常见）。"
    },
    {
      "en-US": "### Error Handling",
      "zh-Hans": "### 错误处理"
    },
    {
      "en-US": "- Malformed JSON from WebSocket clients: log to stderr, continue\n- Unhandled opcodes: close with status 1003\n- Client disconnects: remove from broadcast set\n- `fs.watch` errors: log to stderr, continue\n- No graceful shutdown logic — shell scripts handle process lifecycle via SIGTERM",
      "zh-Hans": "* WebSocket 客户端的格式错误 JSON：记录到 stderr，继续\n* 未处理的操作码：关闭并返回状态 1003\n* 客户端断开连接：从广播集合中移除\n* `fs.watch` 错误：记录到 stderr，继续\n* 没有优雅关闭逻辑 —— shell 脚本通过 SIGTERM 处理进程生命周期"
    },
    {
      "en-US": "## What Changes",
      "zh-Hans": "## 变更内容"
    },
    {
      "en-US": "| Before | After |\n|---|---|\n| `index.js` + `package.json` + `package-lock.json` + 714 `node_modules` files | `server.js` (single file) |\n| express, ws, chokidar dependencies | none |\n| No static file serving | `/files/*` serves from screen directory |",
      "zh-Hans": "| 之前 | 之后 |\n|---|---|\n| `index.js` + `package.json` + `package-lock.json` + 714 个 `node_modules` 文件 | `server.js`（单个文件） |\n| express、ws、chokidar 依赖项 | 无 |\n| 无静态文件服务 | `/files/*` 从屏幕目录提供服务 |"
    },
    {
      "en-US": "## What Stays the Same",
      "zh-Hans": "## 保持不变的内容"
    },
    {
      "en-US": "- `helper.js` — no changes\n- `frame-template.html` — no changes\n- `start-server.sh` — one-line update: `index.js` to `server.js`\n- `stop-server.sh` — no changes\n- `visual-companion.md` — no changes\n- All existing server behavior and external contract",
      "zh-Hans": "* `helper.js` —— 无更改\n* `frame-template.html` —— 无更改\n* `start-server.sh` —— 单行更新：`index.js` 改为 `server.js`\n* `stop-server.sh` —— 无更改\n* `visual-companion.md` —— 无更改\n* 所有现有的服务器行为和外部契约"
    },
    {
      "en-US": "## Platform Compatibility",
      "zh-Hans": "## 平台兼容性"
    },
    {
      "en-US": "- `server.js` uses only cross-platform Node built-ins\n- `fs.watch` is reliable for single flat directories on macOS, Linux, and Windows\n- Shell scripts require bash (Git Bash on Windows, which is required for Claude Code)",
      "zh-Hans": "* `server.js` 仅使用跨平台的 Node 内置模块\n* `fs.watch` 在 macOS、Linux 和 Windows 上对于单个扁平目录是可靠的\n* Shell 脚本需要 bash（Windows 上需要 Git Bash，这是 Claude Code 的要求）"
    },
    {
      "en-US": "## Testing",
      "zh-Hans": "## 测试"
    },
    {
      "en-US": "**Unit tests** (`ws-protocol.test.js`): Test WebSocket frame encoding/decoding, handshake computation, and protocol edge cases directly by requiring `server.js` exports.",
      "zh-Hans": "**单元测试**（`ws-protocol.test.js`）：通过引用 `server.js` 导出的函数，直接测试 WebSocket 帧编码/解码、握手计算和协议边缘情况。"
    },
    {
      "en-US": "**Integration tests** (`server.test.js`): Test full server behavior — HTTP serving, WebSocket communication, file watching, brainstorming workflow. Uses `ws` npm package as a test-only client dependency (not shipped to end users).",
      "zh-Hans": "**集成测试**（`server.test.js`）：测试完整的服务器行为 —— HTTP 服务、WebSocket 通信、文件监视、头脑风暴工作流。使用 `ws` npm 包作为仅测试的客户端依赖项（不交付给最终用户）。"
    }
  ]
}