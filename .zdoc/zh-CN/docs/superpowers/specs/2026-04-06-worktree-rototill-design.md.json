{
  "sourceFile": "docs/superpowers/specs/2026-04-06-worktree-rototill-design.md",
  "sourceLanguage": "en-US",
  "entries": [
    {
      "en-US": "# Worktree Rototill: Detect-and-Defer",
      "zh-Hans": "# 工作树翻耕：检测与延缓"
    },
    {
      "en-US": "**Date:** 2026-04-06\n**Status:** Draft\n**Ticket:** PRI-974\n**Subsumes:** PRI-823 (Codex App compatibility)",
      "zh-Hans": "**日期：** 2026-04-06\n**状态：** 草稿\n**工单：** PRI-974\n**取代：** PRI-823 (Codex 应用兼容性)"
    },
    {
      "en-US": "## Problem",
      "zh-Hans": "## 问题"
    },
    {
      "en-US": "Superpowers is opinionated about worktree management — specific paths (`.worktrees/<branch>`), specific commands (`git worktree add`), specific cleanup (`git worktree remove`). Meanwhile, Claude Code, Codex App, Gemini CLI, and Cursor all provide native worktree support with their own paths, lifecycle management, and cleanup.",
      "zh-Hans": "Superpowers 对工作树管理有主张——特定路径（`.worktrees/<branch>`）、特定命令（`git worktree add`）、特定清理（`git worktree remove`）。与此同时，Claude Code、Codex 应用、Gemini CLI 和 Cursor 都提供原生工作树支持，包括各自的路径、生命周期管理和清理。"
    },
    {
      "en-US": "This creates three failure modes:",
      "zh-Hans": "这产生了三种故障模式："
    },
    {
      "en-US": "1. **Duplication** — on Claude Code, the skill does what `EnterWorktree`/`ExitWorktree` already does\n2. **Conflict** — on Codex App, the skill tries to create worktrees inside an already-managed worktree\n3. **Phantom state** — skill-created worktrees at `.worktrees/` are invisible to the harness; harness-created worktrees at `.claude/worktrees/` are invisible to the skill",
      "zh-Hans": "1. **重复** — 在 Claude Code 上，技能做了 `EnterWorktree`/`ExitWorktree` 已经做的事情\n2. **冲突** — 在 Codex 应用上，技能试图在已管理的工作树内创建工作树\n3. **幻象状态** — 技能创建的位于 `.worktrees/` 的工作树对框架不可见；框架创建的位于 `.claude/worktrees/` 的工作树对技能不可见"
    },
    {
      "en-US": "For harnesses without native support (Codex CLI, OpenCode, Copilot standalone), superpowers fills a real gap. The skill shouldn't go away — it should get out of the way when native support exists.",
      "zh-Hans": "对于没有原生支持的框架（Codex CLI、OpenCode、Copilot standalone），superpowers 填补了实际空白。技能不应消失——它应该在存在原生支持时让路。"
    },
    {
      "en-US": "## Goals",
      "zh-Hans": "## 目标"
    },
    {
      "en-US": "1. Defer to native harness worktree systems when they exist\n2. Continue providing worktree support for harnesses that lack it\n3. Fix three known bugs in finishing-a-development-branch (#940, #999, #238)\n4. Make worktree creation opt-in rather than mandatory (#991)\n5. Replace hardcoded `CLAUDE.md` references with platform-neutral language (#1049)",
      "zh-Hans": "1. 当存在原生工作树系统时，将任务延迟给它们\n2. 继续为缺乏原生支持的框架提供工作树支持\n3. 修复完成开发分支时的三个已知错误（#940、#999、#238）\n4. 使工作树创建变为可选而非强制（#991）\n5. 用平台中性的语言替换硬编码的 `CLAUDE.md` 引用（#1049）"
    },
    {
      "en-US": "## Non-Goals",
      "zh-Hans": "## 非目标"
    },
    {
      "en-US": "- Per-worktree environment conventions (`.worktree-env.sh`, port offsetting) — Phase 4\n- PreToolUse hooks for path enforcement — Phase 4\n- Multi-repo worktree documentation — Phase 4\n- Brainstorming checklist changes for worktrees — Phase 4\n- `.superpowers-session.json` metadata tracking (interesting PR #997 idea, not needed for v1)\n- Hooks symlinking into worktrees (PR #965 idea, separate concern)",
      "zh-Hans": "* 按工作树的环境约定（`.worktree-env.sh`、端口偏移）— 第4阶段\n* 用于路径强制的 PreToolUse 钩子 — 第4阶段\n* 多仓库工作树文档 — 第4阶段\n* 工作树的 Brainstorming 检查清单变更 — 第4阶段\n* `.superpowers-session.json` 元数据跟踪（有趣的 PR #997 想法，但 v1 不需要）\n* 将钩子符号链接进入工作树（PR #965 想法，单独关注点）"
    },
    {
      "en-US": "## Design Principles",
      "zh-Hans": "## 设计原则"
    },
    {
      "en-US": "### Detect state, not platform",
      "zh-Hans": "### 检测状态，而非平台"
    },
    {
      "en-US": "Use `GIT_DIR != GIT_COMMON` to determine \"am I already in a worktree?\" rather than sniffing environment variables to identify the harness. This is a stable git primitive (since git 2.5, 2015), works universally across all harnesses, and requires zero maintenance as new harnesses appear.",
      "zh-Hans": "使用 `GIT_DIR != GIT_COMMON` 来确定“我是否已经在工作树中？”而不是通过嗅探环境变量来识别框架。这是一个稳定的 git 原语（自 git 2.5，2015年起），在所有框架中通用有效，且随着新框架的出现无需维护。"
    },
    {
      "en-US": "### Declarative intent, prescriptive fallback",
      "zh-Hans": "### 声明性意图，规定性后备"
    },
    {
      "en-US": "The skill describes the goal (\"ensure work happens in an isolated workspace\") and defers to native tools when available. It prescribes specific git commands only as a fallback for harnesses without native worktree support. Step 1a comes first and names native tools explicitly (`EnterWorktree`, `WorktreeCreate`, `/worktree`, `--worktree`); Step 1b comes second with the git fallback. The original spec kept Step 1a abstract (\"you know your own toolkit\"), but TDD proved that agents anchor on Step 1b's concrete commands when Step 1a is too vague. Explicit tool naming and a consent-authorization bridge were required to make the preference reliable.",
      "zh-Hans": "技能描述目标（“确保工作在隔离的工作区中进行”）并在原生工具可用时推迟给它们。它只在框架缺乏原生工作树支持时作为后备规定具体的 git 命令。步骤1a优先，明确命名原生工具（`EnterWorktree`、`WorktreeCreate`、`/worktree`、`--worktree`）；步骤1b在后，提供 git 后备方案。原始规范让步骤1a保持抽象（“你了解自己的工具包”），但 TDD 证明，当步骤1a过于模糊时，智能体会锚定步骤1b的具体命令。明确命名工具和同意授权桥接对于使偏好可靠是必要的。"
    },
    {
      "en-US": "### Provenance-based ownership",
      "zh-Hans": "### 基于来源的拥有权"
    },
    {
      "en-US": "Whoever creates the worktree owns its cleanup. If the harness created it, superpowers doesn't touch it. If superpowers created it (via git fallback), superpowers cleans it up. The heuristic: if the worktree lives under `.worktrees/` or `~/.config/superpowers/worktrees/`, superpowers owns it. Anything else (`.claude/worktrees/`, `~/.codex/worktrees/`, `.gemini/worktrees/`) belongs to the harness.",
      "zh-Hans": "谁创建工作树，谁拥有其清理权。如果框架创建了它，superpowers 就不触碰它。如果 superpowers 创建了它（通过 git 后备），那么 superpowers 将其清理。启发式方法：如果工作树位于 `.worktrees/` 或 `~/.config/superpowers/worktrees/` 下，则 superpowers 拥有它。任何其他位置（`.claude/worktrees/`、`~/.codex/worktrees/`、`.gemini/worktrees/`）都归框架所有。"
    },
    {
      "en-US": "## Design",
      "zh-Hans": "## 设计"
    },
    {
      "en-US": "### 1. `using-git-worktrees` SKILL.md Rewrite",
      "zh-Hans": "### 1. `using-git-worktrees` SKILL.md 重写"
    },
    {
      "en-US": "The skill gains three new steps before creation and simplifies the creation flow.",
      "zh-Hans": "该技能在创建之前增加了三个新步骤，并简化了创建流程。"
    },
    {
      "en-US": "#### Step 0: Detect Existing Isolation",
      "zh-Hans": "#### 步骤0：检测现有隔离"
    },
    {
      "en-US": "```bash\nGIT_DIR=$(cd \"$(git rev-parse --git-dir)\" 2>/dev/null && pwd -P)\nGIT_COMMON=$(cd \"$(git rev-parse --git-common-dir)\" 2>/dev/null && pwd -P)\nBRANCH=$(git branch --show-current)\n```",
      "zh-Hans": "```bash\nGIT_DIR=$(cd \"$(git rev-parse --git-dir)\" 2>/dev/null && pwd -P)\nGIT_COMMON=$(cd \"$(git rev-parse --git-common-dir)\" 2>/dev/null && pwd -P)\nBRANCH=$(git branch --show-current)\n```"
    },
    {
      "en-US": "Three outcomes:",
      "zh-Hans": "三种结果："
    },
    {
      "en-US": "| Condition | Meaning | Action |\n|-----------|---------|--------|\n| `GIT_DIR == GIT_COMMON` | Normal repo checkout | Proceed to Step 0.5 |\n| `GIT_DIR != GIT_COMMON`, named branch | Already in a linked worktree | Skip to Step 3 (project setup). Report: \"Already in isolated workspace at `<path>` on branch `<name>`.\" |\n| `GIT_DIR != GIT_COMMON`, detached HEAD | Externally managed worktree (e.g., Codex App sandbox) | Skip to Step 3. Report: \"Already in isolated workspace at `<path>` (detached HEAD, externally managed).\" |",
      "zh-Hans": "| 条件 | 含义 | 操作 |\n|------|------|------|\n| `GIT_DIR == GIT_COMMON` | 正常仓库检出 | 继续步骤0.5 |\n| `GIT_DIR != GIT_COMMON`，命名分支 | 已在链接的工作树中 | 跳至步骤3（项目设置）。报告：“已在 `<path>` 上的隔离工作区中，分支为 `<name>`。” |\n| `GIT_DIR != GIT_COMMON`，分离 HEAD | 外部管理工作树（例如 Codex 应用沙箱） | 跳至步骤3。报告：“已在 `<path>` 上的隔离工作区中（分离 HEAD，外部管理）。” |"
    },
    {
      "en-US": "Step 0 does not care who created the worktree or which harness is running. A worktree is a worktree regardless of origin.",
      "zh-Hans": "步骤0不关心谁的创建了工作树或哪个框架正在运行。工作树就是工作树，无论来源如何。"
    },
    {
      "en-US": "**Submodule guard:** `GIT_DIR != GIT_COMMON` is also true inside git submodules. Before concluding \"already in a worktree,\" check that we're not in a submodule:",
      "zh-Hans": "**子模块保护：** `GIT_DIR != GIT_COMMON` 在 git 子模块内部也为 true。在得出“已在工作树中”结论之前，检查我们是否不在子模块中："
    },
    {
      "en-US": "```bash\n# If this returns a path, we're in a submodule, not a worktree\ngit rev-parse --show-superproject-working-tree 2>/dev/null\n```",
      "zh-Hans": "```bash\n# If this returns a path, we're in a submodule, not a worktree\ngit rev-parse --show-superproject-working-tree 2>/dev/null\n```"
    },
    {
      "en-US": "If in a submodule, treat as `GIT_DIR == GIT_COMMON` (proceed to Step 0.5).",
      "zh-Hans": "如果在子模块中，则视为 `GIT_DIR == GIT_COMMON`（继续步骤0.5）。"
    },
    {
      "en-US": "#### Step 0.5: Consent",
      "zh-Hans": "#### 步骤0.5：同意"
    },
    {
      "en-US": "When Step 0 finds no existing isolation (`GIT_DIR == GIT_COMMON`), ask before creating:",
      "zh-Hans": "当步骤0发现没有现有隔离时（`GIT_DIR == GIT_COMMON`），在创建前询问："
    },
    {
      "en-US": "> \"Would you like me to set up an isolated worktree? This protects your current branch from changes. (y/n)\"",
      "zh-Hans": "> “您希望我设置一个隔离的工作树吗？这将保护您当前的分支免受更改影响。（y/n）”"
    },
    {
      "en-US": "If yes, proceed to Step 1. If no, work in place — skip to Step 3 with no worktree.",
      "zh-Hans": "如果同意，继续步骤1。如果否决，原地工作——跳过步骤3，不建立工作树。"
    },
    {
      "en-US": "This step is skipped entirely when Step 0 detects existing isolation (no point asking about what already exists).",
      "zh-Hans": "当步骤0检测到现有隔离时，此步骤完全跳过（没必要询问已经存在的事情）。"
    },
    {
      "en-US": "#### Step 1a: Native Tools (preferred)",
      "zh-Hans": "#### 步骤1a：原生工具（首选）"
    },
    {
      "en-US": "> The user has asked for an isolated workspace (Step 0 consent). Check your available tools — do you have `EnterWorktree`, `WorktreeCreate`, a `/worktree` command, or a `--worktree` flag? If YES: the user's consent to create a worktree is your authorization to use it. Use it now and skip to Step 3.",
      "zh-Hans": "> 用户已请求隔离工作区（步骤0同意）。检查您可用的工具——您有 `EnterWorktree`、`WorktreeCreate`、`/worktree` 命令或 `--worktree` 标志吗？如果有：用户同意创建的工作树是您使用它的授权。立即使用它并跳过步骤3。"
    },
    {
      "en-US": "After using a native tool, skip to Step 3 (project setup).",
      "zh-Hans": "使用原生工具后，跳至步骤3（项目设置）。"
    },
    {
      "en-US": "**Design note — TDD revision:** The original spec used a deliberately short, abstract Step 1a (\"You know your own toolkit — the skill does not need to name specific tools\"). TDD validation disproved this: agents anchored on Step 1b's concrete git commands and ignored the abstract guidance (2/6 pass rate). Three changes fixed it (50/50 pass rate across GREEN and PRESSURE tests):",
      "zh-Hans": "**设计说明—TDD 修订：** 原始规范使用了简短的、抽象的步骤1a（“你了解自己的工具包——技能不需要命名特定工具”）。TDD 验证推翻了这一点：智能体锚定在步骤1b的具体 git 命令上，忽略了抽象指导（通过率2/6）。三个更改修复了问题（绿色和压力测试下通过率50/50）："
    },
    {
      "en-US": "1. **Explicit tool naming** — listing `EnterWorktree`, `WorktreeCreate`, `/worktree`, `--worktree` by name transforms the decision from interpretation (\"do I have a native tool?\") into factual lookup (\"is `EnterWorktree` in my tool list?\"). Agents on platforms without these tools simply check, find nothing, and fall through to Step 1b. No false positives observed.\n2. **Consent bridge** — \"the user's consent to create a worktree is your authorization to use it\" directly addresses `EnterWorktree`'s tool-level guardrail (\"ONLY when user explicitly asks\"). Tool descriptions override skill instructions (Claude Code #29950), so the skill must frame user consent as the authorization the tool requires.\n3. **Red Flag entry** — naming the specific anti-pattern (\"Use `git worktree add` when you have a native worktree tool — this is the #1 mistake\") in the Red Flags section.",
      "zh-Hans": "1. **明确命名工具** — 通过名称列出 `EnterWorktree`、`WorktreeCreate`、`/worktree`、`--worktree`，将决定从解释（“我有原生工具吗？”）转换为事实查找（“`EnterWorktree` 在我的工具列表中吗？”）。没有这些工具的平台上的智能体简单地检查，找不到，然后回退到步骤1b。未观察到误报。\n2. **同意桥接** — “用户的同意创建的工作树是您使用它的授权”直接处理 `EnterWorktree` 的工具级护栏（“仅当用户明确请求时”）。工具描述覆盖技能指令 (Claude Code #29950)，因此技能必须将用户同意作为工具授权的框架。\n3. **红色标志条目** — 在红色标志部分命名特定的反模式（“当您有原生工作树工具时使用 `git worktree add`——这是号错误”）。"
    },
    {
      "en-US": "File splitting (Step 1b in a separate skill) was tested and proven unnecessary. The anchoring problem is solved by the quality of Step 1a's text, not by physical separation of git commands. Control tests with the full 240-line skill (all git commands visible) passed 20/20.",
      "zh-Hans": "文件拆分（将步骤1b放在一个单独的技能中）经过测试，证明没有必要。锚定问题已经通过步骤1a文本的质量解决，而不是通过物理分离 git 命令。使用完整的240行技能（所有 git 命令可见）的控制测试通过了20/20。"
    },
    {
      "en-US": "#### Step 1b: Git Worktree Fallback",
      "zh-Hans": "#### 步骤1b：Git 工作树后备方案"
    },
    {
      "en-US": "When no native tool is available, create a worktree manually.",
      "zh-Hans": "当没有原生工具可用时，手动创建工作树。"
    },
    {
      "en-US": "**Directory selection** (priority order):",
      "zh-Hans": "**目录选择**（优先级顺序）："
    },
    {
      "en-US": "1. Check for existing `.worktrees/` or `worktrees/` directory — if found, use it. If both exist, `.worktrees/` wins.\n2. Check for existing `~/.config/superpowers/worktrees/<project>/` directory — if found, use it (backward compatibility with legacy global path).\n3. Check the project's agent instruction file (CLAUDE.md, GEMINI.md, AGENTS.md, .cursorrules, or equivalent) for a worktree directory preference.\n4. Default to `.worktrees/`.",
      "zh-Hans": "1. 检查现有 `.worktrees/` 或 `worktrees/` 目录——如果找到，则使用。如果两者都存在，`.worktrees/` 优先。\n2. 检查现有 `~/.config/superpowers/worktrees/<project>/` 目录——如果找到，则使用（向后兼容传统全局路径）。\n3. 检查项目的智能体指令文件（CLAUDE.md、GEMINI.md、AGENTS.md、.cursorrules 或等效文件）中的工作树目录偏好。\n4. 默认使用 `.worktrees/`。"
    },
    {
      "en-US": "No interactive directory selection prompt. The global path (`~/.config/superpowers/worktrees/`) is no longer offered as a choice to new users, but existing worktrees at that location are detected and used for backward compatibility.",
      "zh-Hans": "没有交互式目录选择提示。不再为新用户提供全局路径（`~/.config/superpowers/worktrees/`）作为选择，但会检测并使用该位置的现有工作树，以实现向后兼容。"
    },
    {
      "en-US": "**Safety verification** (project-local directories only):",
      "zh-Hans": "**安全验证**（仅限项目本地目录）："
    },
    {
      "en-US": "```bash\ngit check-ignore -q .worktrees 2>/dev/null\n```",
      "zh-Hans": "```bash\ngit check-ignore -q .worktrees 2>/dev/null\n```"
    },
    {
      "en-US": "If not ignored, add to `.gitignore` and commit before proceeding.",
      "zh-Hans": "如果未忽略，则添加到 `.gitignore` 并在继续前提交。"
    },
    {
      "en-US": "**Create:**",
      "zh-Hans": "**创建：**"
    },
    {
      "en-US": "```bash\ngit worktree add \"$path\" -b \"$BRANCH_NAME\"\ncd \"$path\"\n```",
      "zh-Hans": "```bash\ngit worktree add \"$path\" -b \"$BRANCH_NAME\"\ncd \"$path\"\n```"
    },
    {
      "en-US": "**Hooks awareness:** Git worktrees do not inherit the parent repo's hooks directory. After creating a worktree via 1b, symlink the hooks directory from the main repo if one exists:",
      "zh-Hans": "**钩子感知：** Git 工作树不继承父仓库的钩子目录。通过步骤1b创建的工作树后，如果存在，将主仓库的钩子目录符号链接过去："
    },
    {
      "en-US": "```bash\nif [ -d \"$MAIN_ROOT/.git/hooks\" ]; then\n    ln -sf \"$MAIN_ROOT/.git/hooks\" \"$path/.git/hooks\"\nfi\n```",
      "zh-Hans": "```bash\nif [ -d \"$MAIN_ROOT/.git/hooks\" ]; then\n    ln -sf \"$MAIN_ROOT/.git/hooks\" \"$path/.git/hooks\"\nfi\n```"
    },
    {
      "en-US": "This prevents pre-commit checks, linters, and other hooks from silently stopping when work moves to a worktree. (Idea from PR #965.)",
      "zh-Hans": "这可以防止在工作移至工作树时，预提交检查、linter 和其他钩子静默停止。（想法来自 PR #965。）"
    },
    {
      "en-US": "**Sandbox fallback:** If `git worktree add` fails with a permission error, treat as a restricted environment. Skip creation, work in current directory, proceed to Step 3.",
      "zh-Hans": "**沙箱回退：** 如果 `git worktree add` 因权限错误而失败，则视为受限环境。跳过创建工作，在当前目录工作，继续步骤3。"
    },
    {
      "en-US": "**Step numbering note:** The current skill has Steps 1-4 as a flat list. This redesign uses 0, 0.5, 1a, 1b, 3, 4. There is no Step 2 — it was the old monolithic \"Create Isolated Workspace\" which is now split into the 1a/1b structure. The implementation should renumber cleanly (e.g., 0 → \"Step 0: Detect\", 0.5 → within Step 0's flow, 1a/1b → \"Step 1\", 3 → \"Step 2\", 4 → \"Step 3\") or keep the current numbering with a note. Implementer's choice.",
      "zh-Hans": "**步骤编号说明：** 当前技能有步骤1-4作为平面列表。本重新设计使用0、0.5、1a、1b、3、4。没有步骤2——它是旧的单一“创建隔离工作区”，现已拆分为1a/1b结构。实现应当干净地重新编号（例如，0 → “步骤0：检测”，0.5 → 在步骤0流程内，1a/1b →“步骤1”，3 →“步骤2”，4 →“步骤3”）或保留当前编号并附上注释。由实现者决定。"
    },
    {
      "en-US": "#### Steps 3-4: Project Setup and Baseline Tests (unchanged)",
      "zh-Hans": "#### 步骤3-4：项目设置和基线测试（不变）"
    },
    {
      "en-US": "Regardless of which path created the workspace (Step 0 detected existing, Step 1a native tool, Step 1b git fallback, or no worktree at all), execution converges:",
      "zh-Hans": "无论通过哪条路径创建的在工作空间（步骤0检测到现有、步骤1a原生工具、步骤1b git 回退，或者根本没有工作树），执行汇聚："
    },
    {
      "en-US": "- **Step 3:** Auto-detect and run project setup (`npm install`, `cargo build`, `pip install`, `go mod download`, etc.)\n- **Step 4:** Run the test suite. If tests fail, report failures and ask whether to proceed.",
      "zh-Hans": "* **步骤3：** 自动检测并运行项目设置（`npm install`、`cargo build`、`pip install`、`go mod download` 等）\n* **步骤4：** 运行测试套件。如果测试失败，报告失败并询问是否继续。"
    },
    {
      "en-US": "### 2. `finishing-a-development-branch` SKILL.md Rewrite",
      "zh-Hans": "### 2. `finishing-a-development-branch` SKILL.md 重写"
    },
    {
      "en-US": "The finishing skill gains environment detection and fixes three bugs.",
      "zh-Hans": "完成技能增加了环境检测并修复三个错误。"
    },
    {
      "en-US": "#### Step 1: Verify Tests (unchanged)",
      "zh-Hans": "#### 步骤1：验证测试（不变）"
    },
    {
      "en-US": "Run the project's test suite. If tests fail, stop. Don't offer completion options.",
      "zh-Hans": "运行项目的测试套件。如果测试失败，停止。不提供完成选项。"
    },
    {
      "en-US": "#### Step 1.5: Detect Environment (new)",
      "zh-Hans": "#### 步骤1.5：检测环境（新增）"
    },
    {
      "en-US": "Re-run the same detection as Step 0 in creation:",
      "zh-Hans": "重新运行与创建的步骤0相同的检测："
    },
    {
      "en-US": "```bash\nGIT_DIR=$(cd \"$(git rev-parse --git-dir)\" 2>/dev/null && pwd -P)\nGIT_COMMON=$(cd \"$(git rev-parse --git-common-dir)\" 2>/dev/null && pwd -P)\n```",
      "zh-Hans": "```bash\nGIT_DIR=$(cd \"$(git rev-parse --git-dir)\" 2>/dev/null && pwd -P)\nGIT_COMMON=$(cd \"$(git rev-parse --git-common-dir)\" 2>/dev/null && pwd -P)\n```"
    },
    {
      "en-US": "Three paths:",
      "zh-Hans": "三条路径："
    },
    {
      "en-US": "| State | Menu | Cleanup |\n|-------|------|---------|\n| `GIT_DIR == GIT_COMMON` (normal repo) | Standard 4 options | No worktree to clean up |\n| `GIT_DIR != GIT_COMMON`, named branch | Standard 4 options | Provenance-based (see Step 5) |\n| `GIT_DIR != GIT_COMMON`, detached HEAD | Reduced menu: push as new branch + PR, keep as-is, discard | No merge options (can't merge from detached HEAD) |",
      "zh-Hans": "| 状态 | 菜单 | 清理 |\n|------|------|------|\n| `GIT_DIR == GIT_COMMON`（正常仓库） | 标准4个选项 | 无需清理工作树 |\n| `GIT_DIR != GIT_COMMON`，命名分支 | 标准4个选项 | 基于来源的（见步骤5） |\n| `GIT_DIR != GIT_COMMON`，分离 HEAD | 简化菜单：推送为新分支并创建 PR、保持原样、丢弃 | 无合并选项（无法从分离 HEAD 合并） |"
    },
    {
      "en-US": "#### Step 2: Determine Base Branch (unchanged)",
      "zh-Hans": "#### 步骤2：确定基础分支（不变）"
    },
    {
      "en-US": "#### Step 3: Present Options",
      "zh-Hans": "#### 步骤3：呈现选项"
    },
    {
      "en-US": "**Normal repo and named-branch worktree:**",
      "zh-Hans": "**正常仓库和命名分支工作树：**"
    },
    {
      "en-US": "1. Merge back to `<base-branch>` locally\n2. Push and create a Pull Request\n3. Keep the branch as-is (I'll handle it later)\n4. Discard this work",
      "zh-Hans": "1. 在本地合并回 `<base-branch>`\n2. 推送并创建拉取请求\n3. 保持分支不变（我稍后处理）\n4. 丢弃这项工作"
    },
    {
      "en-US": "**Detached HEAD:**",
      "zh-Hans": "**分离 HEAD：**"
    },
    {
      "en-US": "1. Push as new branch and create a Pull Request\n2. Keep as-is (I'll handle it later)\n3. Discard this work",
      "zh-Hans": "1. 作为新分支推送并创建拉取请求\n2. 保持原样（我稍后处理）\n3. 丢弃这项工作"
    },
    {
      "en-US": "#### Step 4: Execute Choice",
      "zh-Hans": "#### 步骤4：执行选择"
    },
    {
      "en-US": "**Option 1 (Merge locally):**",
      "zh-Hans": "**选项1（在本地合并）：**"
    },
    {
      "en-US": "```bash\n# Get main repo root for CWD safety (Bug #238 fix)\nMAIN_ROOT=$(git -C \"$(git rev-parse --git-common-dir)/..\" rev-parse --show-toplevel)\ncd \"$MAIN_ROOT\"\n\n# Merge first, verify success before removing anything\ngit checkout <base-branch>\ngit pull\ngit merge <feature-branch>\n<run tests>\n\n# Only after merge succeeds: remove worktree, then delete branch (Bug #999 fix)\ngit worktree remove \"$WORKTREE_PATH\"  # only if superpowers owns it\ngit branch -d <feature-branch>\n```",
      "zh-Hans": "```bash\n# Get main repo root for CWD safety (Bug #238 fix)\nMAIN_ROOT=$(git -C \"$(git rev-parse --git-common-dir)/..\" rev-parse --show-toplevel)\ncd \"$MAIN_ROOT\"\n\n# Merge first, verify success before removing anything\ngit checkout <base-branch>\ngit pull\ngit merge <feature-branch>\n<run tests>\n\n# Only after merge succeeds: remove worktree, then delete branch (Bug #999 fix)\ngit worktree remove \"$WORKTREE_PATH\"  # only if superpowers owns it\ngit branch -d <feature-branch>\n```"
    },
    {
      "en-US": "The order is critical: merge → verify → remove worktree → delete branch. The old skill deleted the branch before removing the worktree (which fails because the worktree still references the branch). The naive fix of removing the worktree first is also wrong — if the merge then fails, the working directory is gone and changes are lost.",
      "zh-Hans": "顺序至关重要：合并 → 验证 → 移除工作树 → 删除分支。旧技能在移除工作树之前删除了分支（失败，因为工作树仍引用该分支）。对移除工作树先行的简单修复也是错误的——如果随后合并失败，工作目录就不存在了，更改就会丢失。"
    },
    {
      "en-US": "**Option 2 (Create PR):**",
      "zh-Hans": "**选项2（创建 PR）：**"
    },
    {
      "en-US": "Push branch, create PR. Do NOT clean up worktree — user needs it for PR iteration. (Bug #940 fix: remove contradictory \"Then: Cleanup worktree\" prose.)",
      "zh-Hans": "推送分支，创建 PR。不要清理工作树——用户需要它进行 PR 迭代。（修复错误 #940：移除自相矛盾的“然后：清理工作树”的措辞。）"
    },
    {
      "en-US": "**Option 3 (Keep as-is):** No action.",
      "zh-Hans": "**选项3（保持原样）：** 无操作。"
    },
    {
      "en-US": "**Option 4 (Discard):** Require typed \"discard\" confirmation. Then remove worktree (if superpowers owns it), force-delete branch.",
      "zh-Hans": "**选项4（丢弃）：** 需要键入“discard”确认。然后移除工作树（如果 superpowers 拥有它），强制删除分支。"
    },
    {
      "en-US": "#### Step 5: Cleanup (updated)",
      "zh-Hans": "#### 步骤5：清理（已更新）"
    },
    {
      "en-US": "```\nif GIT_DIR == GIT_COMMON:\n    # Normal repo, no worktree to clean up\n    done\n\nif worktree path is under .worktrees/ or ~/.config/superpowers/worktrees/:\n    # Superpowers created it — we own cleanup\n    cd to main repo root       # Bug #238 fix\n    git worktree remove <path>\n\nelse:\n    # Harness created it — hands off\n    # If platform provides a workspace-exit tool, use it\n    # Otherwise, leave the worktree in place\n```",
      "zh-Hans": "```\nif GIT_DIR == GIT_COMMON:\n    # 普通仓库，没有工作树需要清理\n    done\n\nif worktree path is under .worktrees/ or ~/.config/superpowers/worktrees/:\n    # Superpowers 创建了它——我们负责清理\n    cd to main repo root       # Bug #238 fixes\n    git worktree remove <path>\n\nelse:\n    # Harness 创建了它——不要触碰\n    # 如果平台提供 workspace-exit 工具，请使用它\n    # 否则保留工作树原样\n```"
    },
    {
      "en-US": "Cleanup only runs for Options 1 and 4. Options 2 and 3 always preserve the worktree. (Bug #940 fix.)",
      "zh-Hans": "仅对选项1和4运行清理。选项2和3始终保留工作树。（修复错误 #940。）"
    },
    {
      "en-US": "**Stale worktree pruning:** After any `git worktree remove`, run `git worktree prune` as a self-healing step. Worktree directories can get deleted out-of-band (e.g., by harness cleanup, manual `rm`, or `.claude/` cleanup), leaving stale registrations that cause confusing errors. One line, prevents silent rot. (Idea from PR #1072.)",
      "zh-Hans": "**过期工作树清理：** 在任何 `git worktree remove` 之后，运行 `git worktree prune` 作为自我修复步骤。工作树目录可能被带外删除（例如，被框架清理、手动 `rm` 或 `.claude/` 清理），留下过期的注册信息导致令人困惑的错误。一行代码，防止无声腐败。（想法来自 PR #1072。）"
    },
    {
      "en-US": "### 3. Integration Updates",
      "zh-Hans": "### 3. 集成更新"
    },
    {
      "en-US": "#### `subagent-driven-development` and `executing-plans`",
      "zh-Hans": "#### `subagent-driven-development` 和 `executing-plans`"
    },
    {
      "en-US": "Both currently list `using-git-worktrees` as REQUIRED in their integration sections. Change to:",
      "zh-Hans": "两者目前在其集成部分都将 `using-git-worktrees` 列为“必需（REQUIRED）”。更改为："
    },
    {
      "en-US": "> `using-git-worktrees` — Ensures isolated workspace (creates one or verifies existing)",
      "zh-Hans": "> `using-git-worktrees` — 确保工作区隔离（创建一个或验证已有的）"
    },
    {
      "en-US": "The skill itself now handles consent (Step 0.5) and detection (Step 0), so calling skills don't need to gate or prompt.",
      "zh-Hans": "技能本身现在处理同意步骤（步骤 0.5）和检测步骤（步骤 0），因此调用技能无需进行门控或提示。"
    },
    {
      "en-US": "#### `writing-plans`",
      "zh-Hans": "#### `writing-plans`"
    },
    {
      "en-US": "Remove the stale claim \"should be run in a dedicated worktree (created by brainstorming skill).\" Brainstorming is a design skill and does not create worktrees. The worktree prompt happens at execution time via `using-git-worktrees`.",
      "zh-Hans": "移除过时的声明“应在专用工作树中运行（由 brainstorming 技能创建）”。Brainstorming 是一个设计技能，不创建工作树。工作树提示在执行时通过 `using-git-worktrees` 进行。"
    },
    {
      "en-US": "### 4. Platform-Neutral Instruction File References",
      "zh-Hans": "### 4. 平台无关的指令文件引用"
    },
    {
      "en-US": "All instances of hardcoded `CLAUDE.md` in worktree-related skills are replaced with:",
      "zh-Hans": "所有与工作树相关的技能中硬编码的 `CLAUDE.md` 实例均被替换为："
    },
    {
      "en-US": "> \"your project's agent instruction file (CLAUDE.md, GEMINI.md, AGENTS.md, .cursorrules, or equivalent)\"",
      "zh-Hans": "> “你项目的代理指令文件（CLAUDE.md, GEMINI.md, AGENTS.md, .cursorrules 或等效文件）”"
    },
    {
      "en-US": "This applies to directory preference checks in Step 1b.",
      "zh-Hans": "这适用于步骤 1b 中的目录偏好检查。"
    },
    {
      "en-US": "## Bug Fixes (bundled)",
      "zh-Hans": "## 错误修复（打包）"
    },
    {
      "en-US": "| Bug | Problem | Fix | Location |\n|-----|---------|-----|----------|\n| #940 | Option 2 prose says \"Then: Cleanup worktree (Step 5)\" but quick reference says keep it. Step 5 says \"For Options 1, 2, 4\" but Common Mistakes says \"Options 1 and 4 only.\" | Remove cleanup from Option 2. Step 5 applies to Options 1 and 4 only. | finishing SKILL.md |\n| #999 | Option 1 deletes branch before removing worktree. `git branch -d` can fail because worktree still references the branch. | Reorder to: merge → verify tests → remove worktree → delete branch. Merge must succeed before anything is removed. | finishing SKILL.md |\n| #238 | `git worktree remove` fails silently if CWD is inside the worktree being removed. | Add CWD guard: `cd` to main repo root before `git worktree remove`. | finishing SKILL.md |",
      "zh-Hans": "| 错误 | 问题 | 修复 | 位置 |\n|-----|---------|-----|----------|\n| #940 | 选项 2 的描述说“然后：清理工作树（步骤 5）”，但快速参考说保留它。步骤 5 说“仅适用于选项 1、2、4”，但常见错误部分说“仅选项 1 和 4。” | 从选项 2 中移除清理操作。步骤 5 仅适用于选项 1 和 4。 | finishing SKILL.md |\n| #999 | 选项 1 在移除工作树之前删除分支。`git branch -d` 可能失败，因为工作树仍引用该分支。 | 重新排序为：合并 → 验证测试 → 移除工作树 → 删除分支。必须在移除任何内容之前成功完成合并。 | finishing SKILL.md |\n| #238 | 如果当前工作目录在要移除的工作树内部，`git worktree remove` 会静默失败。 | 添加 CWD 守卫：在执行 `git worktree remove` 之前，`cd` 到主仓库根目录。 | finishing SKILL.md |"
    },
    {
      "en-US": "## Issues Resolved",
      "zh-Hans": "## 已解决问题"
    },
    {
      "en-US": "| Issue | Resolution |\n|-------|-----------|\n| #940 | Direct fix (Bug #940) |\n| #991 | Opt-in consent in Step 0.5 |\n| #918 | Step 0 detection + Step 1.5 finishing detection |\n| #1009 | Resolved by Step 1a — agents use native tools (e.g., `EnterWorktree`) which create at harness-native paths. Depends on Step 1a working; see Risks. |\n| #999 | Direct fix (Bug #999) |\n| #238 | Direct fix (Bug #238) |\n| #1049 | Platform-neutral instruction file references |\n| #279 | Solved by detect-and-defer — native paths respected because we don't override them |\n| #574 | **Deferred.** Nothing in this spec touches the brainstorming skill where the bug lives. Full fix (adding a worktree step to brainstorming's checklist) is Phase 4. |",
      "zh-Hans": "| 问题 | 解决方式 |\n|-------|-----------|\n| #940 | 直接修复（错误 #940） |\n| #991 | 步骤 0.5 中的选择同意 |\n| #918 | 步骤 0 检测 + 步骤 1.5 的 finishing 检测 |\n| #1009 | 通过步骤 1a 解决 — 代理使用本地工具（例如 `EnterWorktree`），这些工具在 harness 原生路径下创建。依赖于步骤 1a 正常工作；请参阅风险。 |\n| #999 | 直接修复（错误 #999） |\n| #238 | 直接修复（错误 #238） |\n| #1049 | 平台无关的指令文件引用 |\n| #279 | 通过检测并推迟解决 — 由于我们不覆盖路径，因此原生路径得到尊重 |\n| #574 | **已推迟。** 本规范中的任何内容均未触及错误所在的 brainstorming 技能。完整的修复（向 brainstorming 的检查清单添加工作树步骤）属于第 4 阶段。 |"
    },
    {
      "en-US": "## Risks",
      "zh-Hans": "## 风险"
    },
    {
      "en-US": "### Step 1a is the load-bearing assumption — RESOLVED",
      "zh-Hans": "### 步骤 1a 是主要假设 — 已解决"
    },
    {
      "en-US": "Step 1a — agents preferring native worktree tools over the git fallback — is the foundation the entire design rests on. If agents ignore Step 1a and fall through to Step 1b on harnesses with native support, detect-and-defer fails entirely.",
      "zh-Hans": "步骤 1a — 代理优先使用本地工作树工具而非 git 后备方案 — 是整个设计的基础。如果代理忽略步骤 1a，而在具有本地支持的 harness 上回退到步骤 1b，则“检测并推迟”将完全失败。"
    },
    {
      "en-US": "**Status:** This risk materialized during implementation. The original abstract Step 1a (\"You know your own toolkit\") failed at 2/6 on Claude Code. The TDD gate worked as designed — it caught the failure before any skill files were modified, preventing a broken release. Three REFACTOR iterations identified the root causes (agent anchoring on concrete commands, tool-description guardrail overriding skill instructions) and produced a fix validated at 50/50 across GREEN and PRESSURE tests. See Step 1a design note above for details.",
      "zh-Hans": "**状态：** 此风险在实施过程中浮现。原始的抽象步骤 1a（“你了解自己的工具包”）在 Claude Code 上以 2/6 失败。TDD 门控按设计工作 — 它在任何技能文件被修改之前捕获了失败，从而防止了错误版本的发布。三次“重新设计”迭代确定了根本原因（代理锁定具体命令，工具说明护栏覆盖了技能指令）并制定了一个在 GREEN 和 PRESSURE 测试中以 50/50 验证的修复。详情请参见上面的步骤 1a 设计说明。"
    },
    {
      "en-US": "**Cross-platform validation:**",
      "zh-Hans": "**跨平台验证：**"
    },
    {
      "en-US": "As of 2026-04-06, Claude Code is the only harness with an agent-callable mid-session worktree tool (`EnterWorktree`). All others either create worktrees before the agent starts (Codex App, Gemini CLI, Cursor) or have no native worktree support (Codex CLI, OpenCode). Step 1a is forward-compatible: when other harnesses add agent-callable worktree tools, agents will match them against the named examples and use them without skill changes.",
      "zh-Hans": "截至 2026-04-06，Claude Code 是唯一具有代理可调用的会话中工作树工具（`EnterWorktree`）的 harness。所有其他工具要么在代理启动前创建工作树（Codex App、Gemini CLI、Cursor），要么没有本地工作树支持（Codex CLI、OpenCode）。步骤 1a 是向前兼容的：当其他 harness 添加代理可调用的工作树工具时，代理将使它们与命名的示例匹配，并在无需技能更改的情况下使用它们。"
    },
    {
      "en-US": "| Harness | Current worktree model | Skill mechanism | Tested |\n|---------|----------------------|-----------------|--------|\n| Claude Code | Agent-callable `EnterWorktree` | Step 1a | 50/50 (GREEN + PRESSURE) |\n| Codex CLI | No native tool (shell only) | Step 1b git fallback | 6/6 (`codex exec`) |\n| Gemini CLI | Launch-time `--worktree` flag, no agent tool | Step 0 if launched with flag, Step 1b if not | Step 0: 1/1, Step 1b: 1/1 (`gemini -p`) |\n| Cursor Agent | User-facing `/worktree`, no agent tool | Step 0 if user activated, Step 1b if not | Step 0: 1/1, Step 1b: 1/1 (`cursor-agent -p`) |\n| Codex App | Platform-managed, detached HEAD, no agent tool | Step 0 detects existing | 1/1 simulated |\n| OpenCode | Detection only (`ctx.worktree`), no agent tool | Step 1b git fallback | Untested (no CLI access) |",
      "zh-Hans": "| Harness | 当前工作树模型 | 技能机制 | 测试情况 |\n|---------|----------------------|-----------------|--------|\n| Claude Code | 代理可调用的 `EnterWorktree` | 步骤 1a | 50/50（GREEN + PRESSURE） |\n| Codex CLI | 无本地工具（仅限 Shell） | 步骤 1b git 后备方案 | 6/6（`codex exec`） |\n| Gemini CLI | 启动时使用 `--worktree` 标志，无代理工具 | 如果使用标志启动，则执行步骤 0；否则执行步骤 1b | 步骤 0：1/1，步骤 1b：1/1（`gemini -p`） |\n| Cursor Agent | 面向用户的 `/worktree`，无代理工具 | 如果用户激活，则执行步骤 0；否则执行步骤 1b | 步骤 0：1/1，步骤 1b：1/1（`cursor-agent -p`） |\n| Codex App | 平台管理，Detached HEAD，无代理工具 | 步骤 0 检测现有状态 | 1/1 模拟 |\n| OpenCode | 仅检测（`ctx.worktree`），无代理工具 | 步骤 1b git 后备方案 | 未测试（无 CLI 访问） |"
    },
    {
      "en-US": "**Residual risks:**",
      "zh-Hans": "**剩余风险：**"
    },
    {
      "en-US": "1. If Anthropic changes `EnterWorktree`'s tool description to be more restrictive (e.g., \"Do not use based on skill instructions\"), the consent bridge breaks. Worth filing an issue requesting that the tool description accommodate skill-driven invocation.\n2. When other harnesses add agent-callable worktree tools, they may use names not in Step 1a's list. The list should be updated as new tools appear. The generic phrasing (\"a worktree or workspace-isolation tool\") provides some forward coverage.",
      "zh-Hans": "1. 如果 Anthropic 更改 `EnterWorktree` 的工具说明，使其限制性更强（例如，“不要基于技能指令使用”），则同意桥梁断开。值得提交一个问题，请求工具说明应适应技能驱动的调用。\n2. 当其他 harness 添加代理可调用的工作树工具时，它们可能使用不在步骤 1a 列表中的名称。应随着新工具的出现更新该列表。通用表述（“一个工作树或工作区隔离工具”）提供了一些前瞻性覆盖。"
    },
    {
      "en-US": "### Provenance heuristic",
      "zh-Hans": "### 来源启发式"
    },
    {
      "en-US": "The `.worktrees/` or `~/.config/superpowers/worktrees/` = ours, anything else = hands off` heuristic works for every current harness. If a future harness adopts `.worktrees/` as its convention, we'd have a false positive (superpowers tries to clean up a harness-owned worktree). Similarly, if a user manually runs `git worktree add .worktrees/experiment` without superpowers, we'd incorrectly claim ownership. Both are low risk — every harness uses branded paths, and manual `.worktrees/` creation is unlikely — but worth noting.",
      "zh-Hans": "`.worktrees/` 或 `~/.config/superpowers/worktrees/` = 我们的，其他任何内容 = 放手 `heuristic works for every current harness. If a future harness adopts`.worktrees/`as its convention, we'd have a false positive (superpowers tries to clean up a harness-owned worktree). Similarly, if a user manually runs`git worktree add .worktrees/experiment`图标`without superpowers, we'd incorrectly claim ownership. Both are low risk — every harness uses branded paths, and manual`.worktrees/\\` 创建不太可能 — 但仍值得注意。"
    },
    {
      "en-US": "### Detached HEAD finishing",
      "zh-Hans": "### Detached HEAD 的 finishing"
    },
    {
      "en-US": "The reduced menu for detached HEAD worktrees (no merge option) is correct for Codex App's sandbox model. If a user is in detached HEAD for another reason, the reduced menu still makes sense — you genuinely can't merge from detached HEAD without creating a branch first.",
      "zh-Hans": "针对 Detached HEAD 工作树的简化菜单（无合并选项）对于 Codex App 的沙箱模型是正确的。如果用户因其他原因位于 Detached HEAD，此简化菜单仍然有意义 — 在没有首先创建分支的情况下，你确实无法从 Detached HEAD 合并。"
    },
    {
      "en-US": "## Implementation Notes",
      "zh-Hans": "## 实施说明"
    },
    {
      "en-US": "Both skill files contain sections beyond the core steps that need updating during implementation:",
      "zh-Hans": "两个技能文件包含超出核心步骤的部分，这些部分在实施期间需要更新："
    },
    {
      "en-US": "- **Frontmatter** (`name`, `description`): Update to reflect detect-and-defer behavior\n- **Quick Reference tables**: Rewrite to match new step structure and bug fixes\n- **Common Mistakes sections**: Update or remove items that reference old behavior (e.g., \"Skip CLAUDE.md check\" is now wrong)\n- **Red Flags sections**: Update to reflect new priorities (e.g., \"Never create a worktree when Step 0 detects existing isolation\")\n- **Integration sections**: Update cross-references between skills",
      "zh-Hans": "* **前言部分**（`name`，`description`）：更新以反映“检测并推迟”行为\n* **快速参考表**：重写以匹配新的步骤结构和错误修复\n* **常见错误部分**：更新或移除引用旧行为的条目（例如，“跳过 CLAUDE.md 检查”现在不正确）\n* **危险信号部分**：更新以反映新的优先级（例如，“当步骤 0 检测到现有隔离时，切勿创建工作树”）\n* **集成部分**：更新技能之间的交叉引用"
    },
    {
      "en-US": "The spec describes *what changes*; the implementation plan will specify exact edits to these secondary sections.",
      "zh-Hans": "本规范描述了*更改内容*；实施计划将指定对这些次要部分的具体编辑。"
    },
    {
      "en-US": "## Future Work (not in this spec)",
      "zh-Hans": "## 未来工作（不在此规范中）"
    },
    {
      "en-US": "- **Phase 3 remainder:** `$TMPDIR` directory option (#666), setup docs for caching and env inheritance (#299)\n- **Phase 4:** PreToolUse hooks for path enforcement (#1040), per-worktree env conventions (#597), brainstorming checklist worktree step (#574), multi-repo documentation (#710)",
      "zh-Hans": "* **第 3 阶段剩余部分：** `$TMPDIR` 目录选项（#666），用于缓存和环境继承的设置文档（#299）\n* **第 4 阶段：** 用于路径执行的 PreToolUse 钩子协议（#1040），每个工作树的环境规范（#597），brainstorming 清单工作树步骤（#574），多仓库文档（#710）"
    }
  ]
}